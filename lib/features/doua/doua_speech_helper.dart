class DouaSpeechHelper {
  static void speakWeb(
    String text, {
    required Function() onEnd,
    required Function(String) onError,
  }) {
    // No-op for mobile
  }

  static void stopWeb() {
    // No-op for mobile
  }
}
